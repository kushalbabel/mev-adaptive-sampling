Sample optimization command:
`python3 optimize.py -t toy_problem -d toy_domain -v`

If you want to run simulation standalone, sample command:
`python3 simulate.py -f vector_data.txt`

To check if a client is stuck:
`cd simulation`
`bash ping_hardhats.sh`

Info about problem generation:
eth_token_tests -> Sushiswap problems
eth_token_tests_uniswapv2 -> UniswapV2 problems
